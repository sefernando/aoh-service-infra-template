# aoh-service-infra-template

This repository serves as an `Infrastrucsture as Code` template for AGIL Ops Hub services.

In general, custom changes are to be made to `values.yaml` of the respective chart.
